{
  "name": "flagsweep-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cucumber-js",
    "test:local": "docker compose -f ../docker-compose.test.yml up app -d --build --wait && BASE_URL=http://localhost:9090 cucumber-js; code=$?; docker compose -f ../docker-compose.test.yml down -v; exit $code",
    "test:docker": "docker compose -f ../docker-compose.test.yml up e2e --build --abort-on-container-exit --exit-code-from e2e; code=$?; docker compose -f ../docker-compose.test.yml down -v; exit $code"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "@playwright/test": "^1.50.0",
    "tsx": "^4.19.0"
  }
}
