---
title: Flagsweep 0.1.0
authors: [audrius]
tags: [release]
draft: true
---

Flagsweep 0.1.0 is the first release: a self-hosted FeatureOps layer on top of the feature flags in Azure App Configuration.

<!-- truncate -->

This post is a draft, so it only shows up under `npm start`. Rewrite it, remove `draft: true`, and add `{to: '/blog/', label: 'Blog', position: 'left'}` to the navbar items in `docusaurus.config.ts` to publish the blog.

The full list of what shipped is in the [changelog](/changelog/).
